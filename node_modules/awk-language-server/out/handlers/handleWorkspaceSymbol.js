"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.getWorkspaceSymbolHandler = void 0;
function getWorkspaceSymbolHandler(context) {
    const { symbols } = context;
    return function handleWorkspaceSymbol(params) {
        const result = [];
        const symbolBuckets = Object.values(symbols);
        for (const sb of symbolBuckets) {
            const matchedNames = [];
            for (const name of sb.keys()) {
                if (name.includes(params.query))
                    matchedNames.push(name);
            }
            result.push(...matchedNames.flatMap((n) => sb.get(n) || []));
        }
        return result;
    };
}
exports.getWorkspaceSymbolHandler = getWorkspaceSymbolHandler;
//# sourceMappingURL=handleWorkspaceSymbol.js.map